{
  "name": "ironman-suit-designer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check:syntax": "find js -name '*.js' -print0 | xargs -0 -n1 node --check",
    "check:diff": "git diff --check",
    "verify": "npm run check:syntax && npm test && npm run check:diff"
  }
}
